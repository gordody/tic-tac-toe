// Re-export types from server for use in frontend client
export type { GameSession, Player, MovePayload, MoveResult, GameConfig, ClientToServerEvents, ServerToClientEvents } from '../../server/src/types';
